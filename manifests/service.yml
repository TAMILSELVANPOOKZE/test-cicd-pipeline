apiVersion: v1
kind: Service
metadata:
    name: tamilselvanpookzetestcicdpipeline
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: tamilselvanpookzetestcicdpipeline